using ProjetoDDD.Sensores.Domain;
using System;
using System.IO;

namespace ProjetoDDD.Sensores.Presentation
{
    public static class ValidaParametrosDeConfiguracao
    {
        //public static void ValidarConfiguracao(this Configuracao conf)
        //{
        //    //if (conf.AppSettings == null)
        //    //    throw new ArgumentException("Grupo de informações de conexão 'AppSettings' não foi informado.");

        //    //if (string.IsNullOrEmpty(conf.AppSettings.ConnectionString))
        //    //    throw new ArgumentException("O campo string de conexão 'ConnectionString' no grupo 'AppSettings' não foi informado!");

        //    //if (string.IsNullOrEmpty(conf.CaminhoArquivoLog))
        //    //    throw new ArgumentException("O caminho do arquivo de log informado em 'CaminhoArquivoLog' não foi informado.");

        //    //if (!File.Exists(conf.CaminhoArquivoLog))
        //    //    throw new ArgumentException("O caminho do arquivo de log informado em 'CaminhoArquivoLog' não existe ou não pode ser acessado.");
        //}
    }
}